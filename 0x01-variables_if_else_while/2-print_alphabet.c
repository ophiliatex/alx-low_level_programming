#include <stdio.h>

/**
 * main -Entry point prints the alphabet in lowercase,
 *
 * Return: 0 (Success)
 */

int main(void)

{

	char ap;

	for (ap = 'a'; ap <= 'z'; ap++)

	{
		putchar(ap);

	}

	putchar('\n');

	return (0);

}
