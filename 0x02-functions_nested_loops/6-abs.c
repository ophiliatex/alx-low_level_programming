#include "main.h"

/**
 * _abs -Entry computes the absolute value of an integer
 * @n: the value to be  checked
 * Return: the absolute value of int
 */

int _abs(int n)

{
if (n >= 0)
{
return (n);
}
return (-n);
}
